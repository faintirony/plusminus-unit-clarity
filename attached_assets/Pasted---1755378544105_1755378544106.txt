колонка со статусом "ПРИБЫЛЬНЫЙ"/"УБЫТОЧНЫЙ" слишком узкая и текст не влезает. А товары с "Не указано" показывают прочерки вместо знака вопроса.

Анализ проблем:
Статус колонка слишком узкая - нужно увеличить ширину
"?" не отображается для товаров без себестоимости
Табы "О товаре" тоже обрезаются
Предлагаемые исправления:
CSS (обновления grid-template-columns)
css

Копировать код
/* Увеличиваем ширину статус-колонки */
.card-content {
    grid-template-columns: 
        40px          /* иконка */
        1fr           /* название */
        80px          /* цена */
        100px         /* себестоимость */
        80px          /* маржа ₽ */
        70px          /* маржа % */
        140px         /* статус (было 120px) */
        130px;        /* табы (было 120px) */
    gap: 12px;        /* уменьшаем gap */
}

/* Статус бейдж */
.profitability-badge {
    padding: 4px 8px;
    border-radius: 12px;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    white-space: nowrap;
    text-align: center;
    width: 100%;
    min-width: 130px; /* Гарантируем место для полного текста */
    box-sizing: border-box;
}

.profitability-badge.needs-cost {
    background: #f7fafc;
    color: #4a5568;
    border: 1px solid #e2e8f0;
    font-size: 16px;
    font-weight: 700;
    width: 32px;
    height: 32px;
    min-width: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    position: relative;
    cursor: help;
    margin: 0 auto;
}

/* Табы компактнее */
.product-tabs {
    display: flex;
    gap: 3px;
    justify-content: center;
    align-items: center;
}

.tab-button {
    padding: 6px 7px;
    border: 1px solid #e2e8f0;
    background: #f8fafc;
    border-radius: 5px;
    font-size: 11px;
    font-weight: 500;
    color: #4a5568;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 3px;
    position: relative;
    min-width: 38px;
    justify-content: center;
}

.tab-text {
    font-size: 10px;
}

/* Адаптивность */
@media (max-width: 1400px) {
    .card-content {
        grid-template-columns: 
            36px          /* иконка */
            1fr           /* название */
            70px          /* цена */
            90px          /* себестоимость */
            70px          /* маржа ₽ */
            60px          /* маржа % */
            120px         /* статус */
            110px;        /* табы */
        gap: 10px;
    }
}

@media (max-width: 1199px) {
    .card-content {
        grid-template-columns: 
            36px          /* иконка */
            1fr           /* название */
            70px          /* цена */
            90px          /* себестоимость */
            70px          /* маржа ₽ */
            60px          /* маржа % */
            32px          /* статус (только ?) */
            96px;         /* табы */
        gap: 8px;
    }

    /* На средних экранах скрываем текст статуса, оставляем только ? */
    .profitability-badge:not(.needs-cost) {
        display: none; /* Временно скрываем полные статусы */
    }

    /* Или сокращаем до иконок */
    .profitability-badge.profitable {
        background: #c6f6d5;
        color: #22543d;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 14px;
        min-width: 28px;
    }

    .profitability-badge.profitable::after {
        content: "✓";
    }

    .profitability-badge.unprofitable {
        background: #fed7d7;
        color: #742a2a;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 14px;
        min-width: 28px;
    }

    .profitability-badge.unprofitable::after {
        content: "✕";
    }
}
JavaScript (исправление логики отображения статуса)
javascript

Копировать код
// Обновляем метод getProfitabilityText
getProfitabilityText(product) {
    if (product.costPrice === null) return '?';
    return product.isProfitable ? 'ПРИБЫЛЬНЫЙ' : 'УБЫТОЧНЫЙ';
}

// Исправляем renderProducts - секция статуса
<!-- Статус прибыльности -->
<div class="profitability-column">
    <span class="profitability-badge ${this.getProfitabilityClass(product)}" 
          id="status-${product.id}" 
          title="${product.costPrice === null ? 'Укажите себестоимость' : ''}">
        ${this.getProfitabilityText(product)}
        ${product.costPrice === null ? '<div class="status-tooltip">Укажите себестоимость для расчета прибыльности</div>' : ''}
    </span>
</div>
Основные изменения:
1. Увеличили статус колонку до 140px на широких экранах
2. На средних экранах - иконки ✓/✕/? вместо полного текста
3. Уменьшили gap между колонками
4. Исправили отображение "?" для товаров без себестоимости

Согласны с таким подходом? Или предпочитаете другое решение для адаптивности?

Д
Вы

согласна


Claude Sonnet 4

Отлично! Вот полный код с исправлениями для Replit:

CSS (полные обновления для styles.css)
css

Копировать код
/* Фиксированная сетка с увеличенными колонками статуса */
.card-content {
    padding: 12px 16px;
    display: grid;
    grid-template-columns: 
        40px          /* иконка */
        1fr           /* название (гибкий) */
        80px          /* цена */
        100px         /* себестоимость */
        80px          /* маржа ₽ */
        70px          /* маржа % */
        140px         /* статус (увеличено) */
        130px;        /* табы (увеличено) */
    gap: 12px;        /* уменьшен gap */
    align-items: center;
    min-height: 52px;
}

/* Иконка товара */
.product-image {
    width: 36px;
    height: 36px;
    background: #f7fafc;
    border-radius: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    justify-self: center;
}

/* Информация о товаре */
.product-info {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-width: 0;
}

.product-title {
    font-size: 14px;
    font-weight: 600;
    color: #2d3748;
    margin-bottom: 2px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    line-height: 1.3;
}

.product-marketplace {
    font-size: 11px;
    color: #718096;
    font-weight: 500;
}

/* Метрики */
.metric-column {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.metric-value {
    font-size: 14px;
    font-weight: 700;
    color: #2d3748;
    line-height: 1.2;
    width: 100%;
    text-align: center;
}

.metric-value.positive {
    color: #38a169;
}

.metric-value.negative {
    color: #e53e3e;
}

.metric-value.undefined {
    color: #a0aec0;
    font-weight: 400;
    font-size: 16px;
}

.metric-label {
    font-size: 9px;
    color: #a0aec0;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    margin-top: 1px;
    font-weight: 500;
    text-align: center;
}

/* Статус прибыльности */
.profitability-column {
    display: flex;
    justify-content: center;
    align-items: center;
}

.profitability-badge {
    padding: 4px 8px;
    border-radius: 12px;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    white-space: nowrap;
    text-align: center;
    width: 100%;
    min-width: 130px;
    box-sizing: border-box;
}

.profitability-badge.profitable {
    background: #c6f6d5;
    color: #22543d;
}

.profitability-badge.unprofitable {
    background: #fed7d7;
    color: #742a2a;
}

.profitability-badge.needs-cost {
    background: #f7fafc;
    color: #4a5568;
    border: 1px solid #e2e8f0;
    font-size: 16px;
    font-weight: 700;
    width: 32px;
    height: 32px;
    min-width: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    position: relative;
    cursor: help;
    margin: 0 auto;
}

.profitability-badge.needs-cost:hover {
    background: #edf2f7;
    border-color: #cbd5e0;
}

/* Тултип для статуса ? */
.status-tooltip {
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    background: #2d3748;
    color: white;
    padding: 6px 10px;
    border-radius: 6px;
    font-size: 12px;
    font-weight: 500;
    white-space: nowrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s;
    z-index: 10;
    margin-bottom: 6px;
}

.status-tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 5px solid transparent;
    border-top-color: #2d3748;
}

.profitability-badge.needs-cost:hover .status-tooltip {
    opacity: 1;
}

/* Табы */
.product-tabs {
    display: flex;
    gap: 3px;
    justify-content: center;
    align-items: center;
}

.tab-button {
    padding: 6px 7px;
    border: 1px solid #e2e8f0;
    background: #f8fafc;
    border-radius: 5px;
    font-size: 11px;
    font-weight: 500;
    color: #4a5568;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 3px;
    position: relative;
    min-width: 38px;
    justify-content: center;
}

.tab-button:hover {
    background: #edf2f7;
    border-color: #cbd5e0;
}

.tab-button.active {
    background: #4299e1;
    color: white;
    border-color: #4299e1;
}

.tab-icon {
    font-size: 12px;
    line-height: 1;
}

.tab-text {
    white-space: nowrap;
    font-size: 10px;
}

/* Тултипы для табов */
.tab-button .tooltip {
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    background: #2d3748;
    color: white;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 10px;
    white-space: nowrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s;
    z-index: 10;
    margin-bottom: 4px;
}

.tab-button .tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 4px solid transparent;
    border-top-color: #2d3748;
}

.tab-button:hover .tooltip {
    opacity: 1;
}

/* Редактируемая себестоимость */
.metric-value.editable {
    cursor: pointer;
    padding: 4px 6px;
    border-radius: 4px;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
    border: 1px solid transparent;
    position: relative;
    width: 100%;
    box-sizing: border-box;
}

.metric-value.editable:hover {
    background: #f7fafc;
    border-color: #e2e8f0;
}

.metric-value.not-specified {
    color: #718096;
    font-style: italic;
    font-weight: 500;
}

.metric-value.not-specified:hover {
    background: #fff5d6;
    border-color: #f59e0b;
}

.edit-icon {
    font-size: 10px;
    color: #a0aec0;
    opacity: 0;
    transition: opacity 0.2s;
}

.metric-value.editable:hover .edit-icon {
    opacity: 1;
}

/* Состояние редактирования */
.metric-value.editing {
    background: #fef5e7;
    border: 2px solid #f6ad55;
    box-shadow: 0 0 0 3px rgba(246, 173, 85, 0.1);
    padding: 2px 4px;
}

.metric-value.editing.not-specified {
    background: #fef3c7;
    border-color: #f59e0b;
    box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
}

.cost-price-input {
    width: 100%;
    max-width: 80px;
    border: none;
    outline: none;
    background: transparent;
    font-size: 14px;
    font-weight: 700;
    color: #2d3748;
    text-align: center;
    padding: 0;
}

.edit-actions {
    position: absolute;
    right: -35px;
    display: flex;
    gap: 2px;
    background: white;
    padding: 2px;
    border-radius: 4px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    z-index: 20;
}

.edit-action-btn {
    background: none;
    border: none;
    cursor: pointer;
    font-size: 12px;
    padding: 2px 4px;
    border-radius: 3px;
    transition: all 0.2s;
    line-height: 1;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.edit-action-btn.save {
    color: #38a169;
    background: #f0fff4;
}

.edit-action-btn.cancel {
    color: #e53e3e;
    background: #fff5f5;
}

.edit-action-btn:hover.save {
    background: #c6f6d5;
}

.edit-action-btn:hover.cancel {
    background: #fed7d7;
}

/* Адаптивность */
@media (max-width: 1400px) {
    .card-content {
        grid-template-columns: 
            36px          /* иконка */
            1fr           /* название */
            70px          /* цена */
            90px          /* себестоимость */
            70px          /* маржа ₽ */
            60px          /* маржа % */
            120px         /* статус */
            110px;        /* табы */
        gap: 10px;
    }

    .profitability-badge {
        min-width: 110px;
    }
}

@media (max-width: 1199px) {
    .card-content {
        grid-template-columns: 
            36px          /* иконка */
            1fr           /* название */
            70px          /* цена */
            90px          /* себестоимость */
            70px          /* маржа ₽ */
            60px          /* маржа % */
            32px          /* статус (иконки) */
            96px;         /* табы */
        gap: 8px;
    }

    .tab-text {
        display: none;
    }

    /* Иконочные статусы для средних экранов */
    .profitability-badge.profitable {
        background: #c6f6d5;
        color: #22543d;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 14px;
        min-width: 28px;
        font-weight: 700;
        position: relative;
        cursor: help;
    }

    .profitability-badge.profitable::before {
        content: "✓";
    }

    .profitability-badge.unprofitable {
        background: #fed7d7;
        color: #742a2a;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 14px;
        min-width: 28px;
        font-weight: 700;
        position: relative;
        cursor: help;
    }

    .profitability-badge.unprofitable::before {
        content: "✕";
    }

    /* Добавляем тултипы для иконочных статусов */
    .profitability-badge.profitable .status-tooltip,
    .profitability-badge.unprofitable .status-tooltip {
        position: absolute;
        bottom: 100%;
        left: 50%;
        transform: translateX(-50%);
        background: #2d3748;
        color: white;
        padding: 4px 8px;
        border-radius: 4px;
        font-size: 10px;
        white-space: nowrap;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.2s;
        z-index: 10;
        margin-bottom: 4px;
    }

    .profitability-badge.profitable:hover .status-tooltip,
    .profitability-badge.unprofitable:hover .status-tooltip {
        opacity: 1;
    }
}

@media (max-width: 767px) {
    .card-content {
        grid-template-columns: 
            32px          /* иконка */
            1fr           /* название */
            60px          /* цена */
            60px          /* маржа ₽ */
            50px          /* маржа % */
            28px          /* статус */
            84px;         /* табы */
        gap: 8px;
    }

    .cost-price-column {
        display: none;
    }

    .product-image {
        width: 32px;
        height: 32px;
        font-size: 16px;
    }

    .product-title {
        font-size: 13px;
    }

    .product-marketplace {
        font-size: 10px;
    }

    .metric-value {
        font-size: 13px;
    }

    .metric-label {
        font-size: 8px;
    }

    .tab-button